# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: mysql2
  encoding: utf8mb4
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password: abc
  host: db

development:
  <<: *default
  database: appserver_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: appserver_test

# CREATE USER 'admin' IDENTIFIED BY 'password';
# GRANT ALL PRIVILEGES ON appserver_production.* TO 'admin'@'%';
production: &production
  <<: *default
  host: TBD
  database: appserver_production
  username: admin
  password: <%= Rails.application.credentials.mysql_admin_password %>

# CREATE USER 'admin' IDENTIFIED BY 'password';
# GRANT ALL PRIVILEGES ON appserver_staging.* TO 'admin'@'%';
staging:
  <<: *production
  host: sample-staging-addonsstack-1v7rc38n-appdbdbcluster-vmrjczf8he2c.cluster-cbc0ceux1sr1.us-east-1.rds.amazonaws.com
  database: appserver_staging
