# https://github.com/hyperupcall/autoenv

alias appserver-connect="docker-compose exec --workdir /src/appserver appserver bash"

alias appserver-connect-root="docker-compose exec --workdir /root --user root appserver bash"
