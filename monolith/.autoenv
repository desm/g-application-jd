# https://github.com/hyperupcall/autoenv

alias appserver-connect="docker-compose exec --workdir /src appserver bash"

alias appserver-connect-root="docker-compose exec --workdir /src --user root appserver bash"
